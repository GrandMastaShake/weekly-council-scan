# Test harness only. The scripts under test are pure stdlib; the market-data
# provider is stubbed in tests/conftest.py so the suite needs no network and
# no yfinance install.
pytest==8.3.3

# Test-time only, for validating .github/workflows/*.yml. The SCRIPTS stay
# stdlib -- truth_check.py in particular parses nothing with this and must
# keep running on a bare interpreter. This buys a real YAML parse in the
# workflow guard instead of a hand-rolled one that would only catch the
# error we already know about.
PyYAML==6.0.2

